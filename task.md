I want to build an application that allows me to:

1. View an MD file rendered properly based on the MD tags
2. Edit the file while vieweing it and automatically saving those changes.
3. Select and comment on text spans on the file and save those comments on the file
   in a form of an inline comment using the pattern <!-- GK: <comment> -->.
4. If I view an MD file with such inline comments, the viewer should render the
   comments.


Ideally, I want to make the application to be a chrome extension that I can load
locally. If not, then it can be a stand-along MacOS application


